{% extends "task_base_layout.html.jinja" %}

{% set task_script = '_soundcheck' %}

{% block exp_task_content %}

    <div id="exp-soundcheck">
        <h2>Sound Check</h2>
        <p>
            Since this experiment involves listening, we need to make sure
            that your browser can play audio, and that you can
            hear it clearly.  Please find a quiet room and/or use
            headphones to listen.
        </p>
        <p>
            When you are ready to start, please listen to this audio
            clip, and type the word you hear.
        </p>
        <div id="exp-soundcheck-controls">
            <div id="exp-soundcheck-player-wrapper">
                <audio id="exp-soundcheck-player" controls preload="auto"
                       src="{{ exp_static }}/audio/_soundcheck.mp3">
                </audio>
            </div>
            <div id="exp-soundcheck-response-wrapper">
                <div id="exp-soundcheck-input">
                    <div id="exp-soundcheck-prompt">
                        What word did you hear?
                    </div>
                    <div>
                        <input id="exp-soundcheck-response"
                               type="text" size="12"
                               value="" disabled>
                    </div>
                </div>
                <div id="exp-soundcheck-submit-btn-wrapper">
                    <button id="exp-soundcheck-submit-btn" type="button"
                            disabled autocomplete="off">
                        Submit answer
                    </button>
                </div>
                <div id="exp-soundcheck-correct-msg">
                    Correct! Click <b>Next</b> to continue.
                </div>
            </div>
        </div>
        <p id="exp-soundcheck-format-error">
            <b>ERROR:</b> The experiment will not work in
            this browser, because the browser cannot play audio of
            type <b>MP3</b>.  Please use a different browser.
        </p>
    </div>

{% endblock %}
